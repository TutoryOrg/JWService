import { Container } from './styled';
import { Text } from 'react-native';

export const DateHeader = () => {
    return (
        <Container>
            <Text>hello</Text>
        </Container>
    );
};
